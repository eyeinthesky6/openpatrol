services:
  openpatrol:
    build: .
    ports: ["8765:8765"]
    environment:
      OPENPATROL_TICK_SECONDS: "0.2"
    volumes:
      - openpatrol-data:/data
    read_only: true
    tmpfs: [/tmp]
    security_opt: [no-new-privileges:true]
    cap_drop: [ALL]
volumes:
  openpatrol-data:
